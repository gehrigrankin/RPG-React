import React from 'react';

import './Option.css'

const Option = (props) => {
    return (
        <div className='Option'>
            <h1>{props.name}</h1>
        </div>
    )
}
export default Option
